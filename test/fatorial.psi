// Função que calcula o fatorial de um valor de entrada

fn fatorial(valor: i64) -> i64 {
    if valor <= 1 {
        return 1;
    }

    valor * fatorial(valor - 1)
}

fn main() {
    let number: i64 = 5;
    let resp: i64 = fatorial(number);
    debug resp;
}